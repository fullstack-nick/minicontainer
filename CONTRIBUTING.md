# Contributing

Changes must preserve the locked boundaries in `PLAN.md`, compile with GCC and Clang without warnings, include proportionate tests, and pass local, CI, exact-artifact GCP deployment, and live-proof gates before a stage is marked complete.

